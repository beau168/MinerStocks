{
    "name": "miner-stocks",
    "version": "1.0.0",
    "description": "StockAnalytics Market Overview",
    "main": "index.html",
    "scripts": {
        "dev": "wrangler dev",
        "build": "shx mkdir -p dist && shx cp index.html dist/",
        "deploy": "wrangler deploy"
    },
    "devDependencies": {
        "shx": "^0.3.4",
        "wrangler": "^4.0.0"
    }
}